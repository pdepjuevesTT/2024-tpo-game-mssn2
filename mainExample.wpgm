import wollok.game.*

import jugador.*
import configuracion.*


program juego {
	gameManager.configuracionIncialTablero()
	gameManager.cambioMenuIncio()

	game.start()

	//Deberia hacer un loop en la que se cumpla esta secuancia hasta que se quiera terminar la partida 
	if(gameManager.condicionDeIncioHaciaCombate()) 
		gameManager.cambioMenuCombate()
	if(gameManager.condidicionDeCombateHaciaIncio())
		gameManager.cambioMenuIncio()
	


}


